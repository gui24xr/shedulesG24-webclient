import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';

const ShedulePage = () => {
    //El id del servicio necesitamos para traer la agenda del mismo con token
    const {sid} = useNavigate()
    return (
        <div>
            
        </div>
    );
}

export default ShedulePage;
